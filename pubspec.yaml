name: regular_dicers_bot
description: A simple command-line application.
version: 1.0.0
# homepage: https://www.example.com

publish_to: none

environment:
  sdk: '>=2.14.0 <3.0.0'

dependencies:
  args: ^2.0.0
  get_it: ^7.0.0
  infrastructure:
    path: infrastructure
  interface:
    path: interface
  logging: ^1.0.0

dev_dependencies:
  common_dev:
    path: common_dev
