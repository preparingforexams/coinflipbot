---
apiVersion: apps/v1
kind: Deployment
metadata:
  namespace: wooglife
  name: rdb-update-handler
  labels:
    app: rdb-update-handler
spec:
  revisionHistoryLimit: 1
  replicas: 1
  strategy:
    rollingUpdate:
      maxSurge: 0
      maxUnavailable: 1
  selector:
    matchLabels:
      app: rdb-update-handler
  template:
    metadata:
      labels:
        app: rdb-update-handler
    spec:
      containers:
        - name: rdb-update-handler
          image: {{ .Values.image }}
          args:
            - handle_updates
          imagePullPolicy: IfNotPresent
          envFrom:
            - configMapRef:
                name: rdb-config
            - secretRef:
                name: rdb-secrets
