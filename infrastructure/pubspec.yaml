name: infrastructure
description: A starting point for Dart libraries or applications.
version: 0.1.0
# homepage: https://www.example.com

publish_to: none

environment:
  sdk: '>=2.14.0 <3.0.0'

dependencies:
  domain:
    path: ../domain
  application:
    path: ../application
  http: ^0.13.0
  logging:
  postgres: ^2.0.0
  telebot:
    path: ../telebot

dev_dependencies:
  common_dev:
    path: ../common_dev
